spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/library?useUnicode=true&characterEncoding=UTF8&serverTimeZone=UTC
      username: root
      password: www.huawei.com

mybatis:
  type-aliases-package: com.deean.springbootssm.bean
  mapper-locations: mappers/*Mapper.xml

server:
  port: 8080
